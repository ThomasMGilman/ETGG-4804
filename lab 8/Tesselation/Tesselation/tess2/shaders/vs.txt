layout(location=0) in vec3 position;
layout(location=1) in vec2 texCoord;
layout(location=2) in vec3 normal;

out vec2 v_texCoord;
out vec3 v_normal;
out vec3 v_position;

void main(){
	//v_position = position;
	//v_normal = normal;
	//v_texCoord = texCoord;

	vec4 p = vec4(position, 1.0) * worldMatrix;
	v_position = p.xyz;

	p *= viewMatrix * projMatrix;
	gl_Position = p;

	vec4 n = vec4(normal, 0.0) * worldMatrix;
	v_normal = n.xyz;
	
	v_texCoord = texCoord;
}